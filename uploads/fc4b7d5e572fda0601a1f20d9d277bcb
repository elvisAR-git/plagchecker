const express = require('express');

const app = express();

app.use(express.static('./dist/SampleApp'));

app.get('/*', (req, res) =>
    res.sendFile('index.html', {root: 'dist/SampleApp/'}),
);

app.listen(process.env.PORT || 8080);

